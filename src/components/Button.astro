---
//Datos para cada componente 
interface Props {
  title?: string;
  link?: string;
  data_aos?: string;
}

//Extraemos el dato title y content de la respuesta y si no hay, que muestre por defecto los valores 
const { title = "Título por defecto" , link = "#" , data_aos} = Astro.props;
---

<div data-aos={data_aos} >
  <a data-section="contactos" href={link}
  type="button" 
  class="bg-black text-white text-center text-sm md:text-xl font-semibold p-3 md:px-5 py-2 rounded-2xl shadow-md transition-all duration-300 ease-in-out transition-all duration-500 hover:scale-105 hover:shadow-2xl hover:rotate-1">{title}</a>
</div>
