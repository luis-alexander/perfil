---
//Datos para cada componente 
interface Props{
    skill?: string;
    imagen?: string;
}

//Extraemos el dato title y content de la respuesta y si no ahy, que muestre por defecto los valores 
const {skill, imagen = "Skill"} = Astro.props 
---

<div class="bg-gray-700 text-white p-2 rounded-md">
    <div class="flex flex-row justify-between content-center text-sm md:text-xl mb-2">
        <img class="w-20 h-20 md:w-20 md:h-20 mx-4 rounded-xl" src={imagen} alt="">
        <p class=" bg-sky-700 p-5 border-4 border-sky-500 shadow-lg shadow-cyan-500/50 hover:scale-105 hover:shadow-2xl hover:rotate-1 transition-all duration-500 w-64 md:w-80 text-center rounded-xl">{skill}</p>
    </div>
</div>

